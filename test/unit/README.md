# Unit Tests for Passive Acoustic Surveillance

For unit tests, `check` library is used so that make sure that you have it:

```bash
$ sudo apt install check
```

The tests are being applied the important functions in source file using:

```bash
$ make test-analysis			# Unit tests for ../../src/analysis.c
```

